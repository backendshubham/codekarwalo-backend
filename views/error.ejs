<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= status %> - <%= message %> | CodeKarwalo</title>
    
    <!-- CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    
    <style>
        :root {
            --primary-color: #2563eb;
            --secondary-color: #1e40af;
            --text-color: #1f2937;
            --light-bg: #f3f4f6;
        }
        
        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--light-bg);
            min-height: 100vh;
            display: flex;
            align-items: center;
        }
        
        .error-container {
            max-width: 600px;
            width: 100%;
            margin: 0 auto;
            padding: 2rem;
            text-align: center;
        }
        
        .error-card {
            background-color: white;
            border-radius: 1rem;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            padding: 3rem 2rem;
        }
        
        .error-icon {
            font-size: 5rem;
            margin-bottom: 1.5rem;
        }
        
        .error-code {
            font-size: 4rem;
            font-weight: bold;
            color: var(--primary-color);
            margin-bottom: 1rem;
        }
        
        .error-message {
            font-size: 1.5rem;
            color: var(--text-color);
            margin-bottom: 2rem;
        }
        
        .error-description {
            color: #6b7280;
            margin-bottom: 2rem;
        }
        
        .btn-primary {
            background-color: var(--primary-color);
            border-color: var(--primary-color);
            padding: 0.75rem 1.5rem;
        }
        
        .btn-primary:hover {
            background-color: var(--secondary-color);
            border-color: var(--secondary-color);
        }
        
        .btn-outline-primary {
            color: var(--primary-color);
            border-color: var(--primary-color);
            padding: 0.75rem 1.5rem;
        }
        
        .btn-outline-primary:hover {
            background-color: var(--primary-color);
            border-color: var(--primary-color);
        }
    </style>
</head>
<body>
    <div class="error-container">
        <div class="error-card">
            <% if (status === 404) { %>
                <i class="fas fa-map-signs error-icon text-primary"></i>
            <% } else if (status === 403) { %>
                <i class="fas fa-lock error-icon text-warning"></i>
            <% } else if (status === 500) { %>
                <i class="fas fa-exclamation-triangle error-icon text-danger"></i>
            <% } else { %>
                <i class="fas fa-exclamation-circle error-icon text-primary"></i>
            <% } %>
            
            <div class="error-code"><%= status %></div>
            <div class="error-message"><%= message %></div>
            
            <div class="error-description">
                <% if (status === 404) { %>
                    The page you're looking for doesn't exist or has been moved.
                <% } else if (status === 403) { %>
                    You don't have permission to access this resource.
                <% } else if (status === 500) { %>
                    Something went wrong on our end. Please try again later.
                <% } else { %>
                    An unexpected error occurred. Please try again.
                <% } %>
            </div>
            
            <div class="d-flex justify-content-center gap-3">
                <a href="javascript:history.back()" class="btn btn-outline-primary">
                    <i class="fas fa-arrow-left me-2"></i>
                    Go Back
                </a>
                <a href="/" class="btn btn-primary">
                    <i class="fas fa-home me-2"></i>
                    Go Home
                </a>
            </div>
        </div>
    </div>
</body>
</html> 